import { strTitle } from '../../../helpers/str-title';
import { module, test } from 'qunit';

module('Unit | Helper | str title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = strTitle(42);
  assert.ok(result);
});
